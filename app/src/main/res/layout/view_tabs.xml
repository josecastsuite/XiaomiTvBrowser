<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:parentTag="android.widget.RelativeLayout">
  <androidx.recyclerview.widget.RecyclerView
      android:id="@+id/rvTabs"
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      android:layout_above="@+id/vSpacer"
      android:layout_toStartOf="@+id/btnAdd"
      android:layout_weight="1"
      android:background="@color/chrome_tab_bar_bg"
      />
  <Button
      android:id="@+id/btnAdd"
      android:layout_width="32dp"
      android:layout_height="32dp"
      android:layout_above="@+id/vSpacer"
      android:layout_alignParentEnd="true"
      android:layout_centerVertical="true"
      android:layout_marginStart="5dp"
      android:layout_marginEnd="8dp"
      android:background="@drawable/button_bg_selector"
      android:fontFamily="serif"
      android:padding="0dp"
      android:stateListAnimator="@null"
      android:text="+"
      android:textSize="26sp"
      tools:ignore="HardcodedText"
      android:gravity="center"
      android:nextFocusUp="@+id/ibMenu"
      android:nextFocusDown="@+id/ibCloseTab"
      />
  <View
      android:id="@+id/vSpacer"
      android:layout_width="match_parent"
      android:layout_height="10dp"
      android:layout_alignParentBottom="true"
      android:background="@color/top_bar_background2" />
</merge>